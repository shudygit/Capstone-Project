.venv/
venv/
__pycache__/
*.pyc
results/
figures/
.pytest_cache/
.DS_Store

# MNIST ships with the repo so the project runs fully offline.
# Keep the compressed source files; ignore the large extracted copies,
# which are regenerated locally on first run.
data/*
!data/MNIST/
data/MNIST/*
!data/MNIST/raw/
data/MNIST/raw/*
!data/MNIST/raw/*.gz

# Throwaway experiment output dirs (regenerated locally, like results/)
results_eps/
results_tmp3/
results_tuned/
